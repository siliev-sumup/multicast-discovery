apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: mission
  name: mission
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mission
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: mission
    spec:
      containers:
      - image: registry.gitlab.com/simeon.iliev/nummi/mission-service:latest
        name: mission-service
        imagePullPolicy: Always
        resources: {}
      imagePullSecrets:
        - name: regcred
status: {}
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: mission
  name: mission
spec:
  ports:
  - name: stage
    port: 8080
    protocol: TCP
  selector:
    app: search
  type: ClusterIP
status:
  loadBalancer: {}
